# This is a draft.

# No need to include xrdb -remove if all the files in the registry dir only contain the standard set of color parameters.

xrdb -merge ~/.Xresources

# A Like everytime the switchboard file is updated or saved, this script is run automatically...
